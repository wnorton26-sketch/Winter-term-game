# Requirements for Card Battler Game
# Core dependencies (Python standard library only - no external deps required!)
# The game runs on pure Python 3.7+

# Optional dependencies for enhanced visual interfaces:
flask>=2.0.0  # For web GUI and HTTP API integration with Godot
pygame>=2.0.0  # For advanced Pygame GUI (optional)

# For development/testing (optional):
# pytest>=7.0.0  # For running tests
# black>=22.0.0  # For code formatting
# pylint>=2.12.0  # For linting
